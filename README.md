# 三分天下 · 风云牌局

原创视觉、纯前端、离线可玩的三国题材策略卡牌游戏。

## 开始游戏

直接打开根目录的 `index.html`。该文件约 1.77 MiB，已经内嵌：

- 全部游戏逻辑、界面样式和程序化音效
- 24 张经过许可审计的历史人物画像
- 完整的图片来源与许可账本

不需要安装依赖、启动服务器或下载其他资源，也可以直接部署到 GitHub Pages。

> 本项目不隶属于或代表任何商业桌游、电子游戏及其权利人；不使用第三方游戏的标志、界面截图、卡面或音频。

## 仓库结构

```text
index.html   完整独立游戏
README.md    使用、版权与审计说明
.gitignore   本地系统文件忽略规则
```

最新开发源码、自动化测试、开放资产账本和构建工具保存在 Git 历史提交 `218649a` 中；当前分支是面向游玩和 GitHub Pages 的发行形态。

## 已完成的验证

- 83/83 规则与架构单元测试通过。
- 60 局常规模拟与 500 局压力模拟均为 0 异常、0 牌数问题、0 未结束。
- 1280×720 桌面端与 390×844 移动端真实浏览器验收通过。
- 单文件版无外部脚本、样式或图片请求，控制台 0 error / 0 warning。
- 角色距离、攻击范围、即时速度控制、最近五条互动事件与内嵌许可账本均已验证。

## 进一步全面审计：十项高价值改进

以下不是泛泛的“重构建议”，而是按当前代码路径、规模和实测结果得到的下一阶段优先事项。

1. **将互动记录升级为结构化事件。** 当前角色最近事件通过中文战报中的角色名识别参与者，改文案可能破坏关联。应由引擎直接产生 `{actorId, targetIds, action, cardId, skillId, amount}`，UI 只负责本地化展示。

2. **拆分 UI 巨型模块。** 构建前的 `70-ui.js` 已达到 1322 行，同时负责渲染、弹窗、HumanAgent、战报和输入映射。应拆成 Renderer、Dialogs、InteractionController、ActivityView，并通过只读视图模型通信。

3. **把卡牌效果改成数据驱动注册表。** `30-cardeffects.js` 约 915 行，合法性判断和结算容易跨函数漂移。每张牌应拥有统一的 `canUse / legalTargets / resolve / aiValue` 契约，并以共享校验器服务人类与 AI。

4. **把引擎流程拆成可序列化状态机。** `40-engine.js` 约 1001 行，大量流程依赖异步函数调用栈。显式的 phase/action/effect 状态机能支持暂停、恢复、调试和确定性重放，也能降低异步中途状态难以保存的问题。

5. **实现存档和可重放对局。** 当前只有随机种子，没有持久化行动日志。保存 seed、版本号和规范化决策序列后，可以中途续局、分享精彩对局，并把线上 bug 还原成固定回归用例。

6. **系统性校准身份 AI。** 最近 500 局压力结果为主忠方 177、反贼 315、内奸 8，内奸胜率约 1.6%，明显缺乏竞争力。应建立按人数、身份、武将的胜率矩阵，并优化内奸的局势控制、救主阈值和残局决策。

7. **统一可取消的动画调度器。** 当前动画存在较多独立 `setTimeout`，速度切换只影响后续时长，重开或关闭弹窗时也难以集中取消。应使用单一时间线、取消令牌和逻辑时间倍率，使暂停、跳过动画和重开安全一致。

8. **增加“卡牌特定作用范围”预览。** 当前距离标签展示普通攻击范围，但顺手牵羊、借刀杀人、全体锦囊等采用不同目标规则。选中卡牌后应在每个角色上直接显示“合法 / 距离不足 / 技能免疫 / 已有同名延时牌”等具体原因。

9. **补齐完整无障碍交互。** 已有键盘焦点和 ARIA，但弹窗尚需焦点锁定、关闭后焦点归还、事件播报节流，以及移动端替代 hover/title 的详情入口。这会同时改善键盘、读屏和触屏体验。

10. **建立源码分支到发行文件的 CI。** 当前仓库刻意只保留发行版，直接编辑 1.76 MiB 的生成 HTML 不可维护。建议保留 `source` 分支或独立源码仓库，由 GitHub Actions 执行规则测试、500 局抽样、资产许可检查、浏览器冒烟测试，再自动生成并发布根目录 `index.html`。

## 本轮已经开始实施

- **结构化事件第一阶段：** 日志统一带 `schema` 和 `participantIds`；出牌、计策、伤害、恢复、救援和装备额外记录 `actorId / targetIds / action / cardName / amount`。旧技能消息保留兼容路径，后续可逐项迁移。
- **UI 职责拆分：** 原 1322 行 UI 文件降至约 1051 行；结构化战报/角色互动和弹窗/图鉴/设置已成为独立模块。
- **可序列化执行边界：** 引擎快照包含 RNG、UID、牌区、玩家、当前阶段和 `idle / resolvingPhase / waitingForDecision` 执行状态，为存档和重放提供版本化 JSON 契约。
- **可取消动画时间线：** 动画延迟、帧回调和清理进入命名 scope；取消时会释放等待 Promise，不再只清空未执行队列。
- **内奸动态制衡：** AI 根据反贼与主忠总战力选择压制对象，多人阶段优先保留主公作为最终对手。同一批 500 个种子中，内奸胜场由 8 提升至 20，提升 150%。

## 新一轮审计发现的其他高价值方向

11. **按人数建立平衡矩阵。** 仅看 8 人局会掩盖 3–7 人局的身份与武将偏差；应按人数、身份、武将记录置信区间，而不是只设一个总胜率目标。

12. **为 AI 增加决策解释与难度档位。** 让 AI 输出候选动作、评分和最终原因，可直接诊断误判；简单难度可加入噪声，困难难度再启用手牌概率与两步搜索。

13. **为状态快照设计迁移器。** 一旦支持存档，卡牌键、技能键或字段变化都会使旧存档失效；需要 `schemaVersion`、逐版本迁移和不兼容提示。

14. **合并全体事件噪声。** 南蛮入侵、万箭齐发、五谷丰登会连续产生大量相似战报；应形成一个可展开的父事件，默认摘要结果，避免角色事件条被单次群体牌刷满。

15. **消除剩余本地化文本回退。** 尚未迁移的部分技能消息仍依靠角色名识别参与者；应让每个技能触发器声明 actor、targets、skillId 和结果，最终完全取消文本解析。

16. **优化单文件冷启动。** 24 张 Base64 图片会随 HTML 一次进入内存；可以在保持单文件的前提下使用 Blob URL、延迟解码和按本局武将预热，降低移动设备首屏解码峰值。

## 资产与版权

人物画像来自 Wikimedia Commons：23 张为 Public Domain；孙尚香画像为 CC BY-SA 4.0，作者 Wang Hui 王翙（1736–1795）。完整逐项账本已内嵌在游戏的“来源与许可”页面中。

项目代码本身尚未声明开源许可证。公开 GitHub 仓库不等于自动授予复制、修改或再分发代码的许可；如需接受外部贡献或允许复用，应在发布前明确选择代码许可证。
